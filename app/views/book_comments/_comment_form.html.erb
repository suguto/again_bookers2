<div class="form-group">
  <%= form_with(model: [book, comment]) do |f| %>
    <%= f.label :comment, "コ・メ・ン・トしろ" %>
    <%= f.text_area :comment, class: "form-control" %><br>
    <%= f.submit "送信！！" %>
    <% end %>
</div>